<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadiusRatio="2"
    android:shape="ring"
    android:thickness="30dp"
    android:useLevel="false">
    <solid android:color="@color/colorHomepageBackground"/>
    <stroke
        android:width="2dp"
        android:color="@color/colorPersonThumbnailBoarder"/>
</shape>