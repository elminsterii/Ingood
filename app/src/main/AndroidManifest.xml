<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fff.ingood">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainFlowActivitys.ReigisterFlow.RegisterPrimaryPageActivity"
            android:label="@string/sign_up"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.ReigisterFlow.RegisterLocationPageActivity"
            android:label="@string/sign_up"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.ReigisterFlow.RegisterInterestPageActivity"
            android:label="@string/sign_up"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.SignInPageActivity"
            android:label="@string/sign_in"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.HomePageActivity"
            android:label="@string/home_page"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.LogOutPageActivity"
            android:label="Log Out"
            android:screenOrientation="portrait" >
        </activity>

        <activity
            android:name=".MainFlowActivitys.UpdatePageActivity"
            android:label="Update"
            android:screenOrientation="portrait" >
        </activity>

    </application>

</manifest>